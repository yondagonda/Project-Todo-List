function popup() {
  const add = document.querySelector('.add-todo');
  add.addEventListener('click', () => {
    console.log('wassup');
    // set the form display: block   or something
    // get input values from popup, then insert into object?
    // also wrap this function in a module pattern? and have to export the module pattern function only instead?
  });
}

export default popup;
